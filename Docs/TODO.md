# TODO

## 当前开发总原则

从当前版本开始，后续所有功能开发都必须优先服务于以下主展示场景：

1. 主动求助 / 模拟跌倒后的闭环响应
2. 长时间静止无响应的异常确认与升级
3. 断网或市电异常下的本地自治与日志补传
4. MQ 气体风险的本地确认、告警与 IoT 上报

约束如下：

- 不再以“继续堆更多传感器”作为优先目标
- 不再以房间精细标定、开放式语音对话或复杂 AI 识别作为近期主线
- 不再以“先把人体异常检测做准”作为近期第一优先级
- 状态机、TFT 页面、语音提示、继电器联动、HA 展示、日志设计都必须直接支撑上述场景
- 新增功能若不能增强这些场景的演示闭环，应降低优先级
- 遥控触发接口必须纳入当前构建目标，用于稳定演示场景一
- 低功耗设计必须纳入后续开发计划，新增功能需评估其对最小运行模式和断电续航的影响
- 当前版本整体按“AI+IoT 闭环项目”推进，优先保证上报、联动、确认、升级、日志和展示稳定；AI 先作为风险辅助和事件解释层，不直接绕过本地状态机

详细需求说明见 `Docs/demo_scenarios_requirements.md`

6.30 前的模块拆解、CRUD、重构范围、人员职责与每日计划见 `Docs/implementation_breakdown_to_0630.md`

## Phase 0：工程地基

- [x] 创建项目目录结构
- [x] 创建基础文档骨架
- [x] 导入 GitHub 下载的 CubeMX/CMake 基础工程
- [x] 安装 STM32Cube FW_U5 V1.8.0 固件包
- [x] 初始化 CubeMX 工程
- [x] 配置 CLion CMake 构建
- [x] 跑通 LED + UART 日志：LED 已实测，USART1 日志已通过 COM6 验证

## Phase 1：MVP 闭环

- [x] LED 点灯测试：`PC7 / LD1` 已烧录上板验证，正常闪烁
- [x] USART1 调试串口 CubeMX 配置与日志验证
- [x] I2C1 总线扫描：PB8/PB9 初始化与空总线扫描已验证
- [x] BME280 环境采集：`0x76 / 0x60`，温湿度/气压日志已上板验证
- [x] PIR GPIO 输入：`A3 / PB0` 已上板触发验证
- [x] Rd-03 V2 OT2 人体存在检测：`A4 / PC1` 已上板触发验证
- [x] Rd-03 V2 UART 协议验证：`CN10 pin 32 / 黑色排母外侧列倒数第二孔 / PB10 -> RX`、`CN10 pin 34 / 黑色排母外侧列最下面孔 / PB11 <- OT1`；`COM6-115200.log` 已验证状态、距离和 32 个距离门能量
- [x] Rd-03 V2 雷达特征层 V1：从 `presence / distance / 32 gate_energy` 计算 zone、peak gate、energy sum、motion score、occupied/still seconds，并在 TFT 显示距离与区域
- [x] Rd-03 V2 USART3 RX DMA：USART3_RX 已升级为 `GPDMA1 Channel 1` + Receive-to-Idle，驱动使用 DMA 接收缓冲 + 软件环形缓冲，避免主循环/TFT/日志阻塞时丢帧
- [x] MQ 气体模块 ADC AO 采样：`A2 / PC3 / ADC1_IN4` 已重新上板验证；当前使用 32 次采样去极值、EMA 滤波和运行基线/增量日志，TFT / Wi-Fi / HA 使用 ppm 估算值，mV 链路保留在本地调试日志
- [ ] 2.0 英寸 240 x 320 TFT 显示：SPI1、GPIO、ST7789 默认驱动和本地状态页已实现；ILI9341 试验更差，当前回到 ST7789。纯色诊断确认长连续填充会失步，已改为分块写入，待正式状态页上板复验和坐标/颜色微调
- [ ] OLED / SSD1306 显示：备选，当前不计划使用
- [x] 蜂鸣器 TIM1 PWM 告警：无源三针模块使用 `D6 / PE9 / TIM1_CH1`，TIM1 2 kHz PWM 已接入；COM6 `b` 可单独测试蜂鸣器
- [x] HW-280 四路继电器本地验收：`D3/D4/D5/A5` 高电平触发和低压 LED 负载已上板验证，COM6 `r/t/y/u` 可控制继电器
- [x] HW-280 四路继电器状态机联动验收：代码已实现并上板复验 `manual mask | auto mask`，继电器 1 随告警/等待确认自动打开，继电器 2 随网络离线自动打开；现场已确认跳帽必须处于 `H / High Level Trigger`
- [x] STM32 USART2 TX DMA 状态发送：代码已实现，6.30 冻结版状态帧为 `S,seq,temp,hum,gas,presence,risk,relay_state_mask,cloud_perm_mask`
- [x] ESP8266 UART CSV 解析与 MQTT JSON 转换：代码已实现
- [x] ESP8266 假数据 -> Mosquitto -> Home Assistant 基础实体展示：已验证
- [ ] 真实传感器数据 -> STM32 -> ESP8266 -> MQTT -> Home Assistant 全链路验收
- [x] STM32 临时风险分级：仅用于链路测试，不作为正式产品规则
- [ ] STM32 正式风险状态机与事件码：代码第一版已接入 `Modules/app`，待上板验证 `D` 命令触发、`E` 事件上报和超时升级
- [ ] 雷达房间区域标定：按实际安装位置记录 `distance_cm / zone / peak_gate / motion_score`

## Phase 2：比赛版 1.0

### P0：必须优先完成

- [ ] 共用风险状态机：SOS/跌倒、久静、气体风险、离线自治的触发、提醒、确认、超时升级、清除；代码第一版已接入，待烧录联调
- [ ] 场景一：主动求助 / 模拟跌倒闭环；已支持 dashboard `D` 命令触发、确认、清除和超时升级，待上板联调
- [ ] 遥控触发接口纳入构建目标，并接入场景一事件触发链路
- [ ] 场景二：长时间静止无响应闭环；已支持雷达 `still_seconds` 达阈值后进入确认流程，待实际场景调阈值
- [ ] 场景三：断网离线下本地自治与恢复补传；已支持 demo 网络离线/恢复事件，日志补传仍需继续完善
- [ ] 场景四：MQ 气体风险闭环；已支持 `gas_ppm_est >= 100` 自动进入确认流程，`3` 可模拟触发，待上板验收
- [x] 本地确认输入：`D0 / PG8 / SOS_BUTTON` 和 `D1 / PG7 / ACK_BUTTON` 已配置并用短接 GND 方式验收；实体 6 脚自锁按钮后续再整理，语音确认可后补
- [ ] TFT 页面适配核心场景状态展示
- [x] 蜂鸣器 / 本地告警联动：无源蜂鸣器第一版已按 app 状态发声；语音提示后补
- [ ] 关键事件日志与最近事件缓存；主控端已实现最近 16 条事件环形缓冲，待补传策略完善
- [ ] MQTT / Home Assistant 核心场景状态展示与自动化联动；STM32 `E` 事件帧发送已实现，待 ESP8266/MQTT/面板联调

### P1：高收益增强

- [ ] 事件分级与事件码细化
- [ ] 异常场景演示脚本
- [x] 继电器本地联动闭环：relay1 告警灯和 relay2 离线灯已接入本地状态机并上板复验；ACK/clear 会清空手动/云端 `manual mask`，本地确认可关闭远程打开的演示灯
- [ ] 米家设备经 HA 联动展示
- [ ] 调试日志沉淀
- [ ] 答辩素材整理
- [ ] 在线 IoT 展示面板整理：状态、告警、日志、联动结果统一展示
- [ ] 云端 fancy 展示面板：突出状态、告警、时间线、联动结果
- [ ] 云端简洁控制面板：用于主动触发场景、确认、清除和继电器控制

### P2：后续增强，不抢主闭环资源

- [ ] ESP32-S3 + RuView Wi-Fi 人体存在/区域感知子系统部署
- [ ] 语音识别增强接入
- [ ] 市电异常 + 备用供电模式
- [ ] 低功耗架构设计：明确全功能模式、演示模式、最小运行模式
- [ ] 低功耗实现：梳理高功耗外设开关策略，评估雷达、显示、联网、语音模块的功耗分层
- [ ] 断电续航设计：为后续电池供电、日志保留和关键告警保活预留接口
- [ ] 精确人体检测、位置变化辅助判定和高置信度异常识别
- [ ] AI+IoT 路线：本地 AI 作为 `risk_hint`，云端 AI 做趋势分析、告警解释和联动建议；详见 `Docs/ai_iot_roadmap.md`
- [ ] 场景检测路线：按 `Docs/scenario_detection_catalog.md` 先完成离开/返回、夜间异常活动、环境舒适度风险，再考虑接入视觉/IMU/高级雷达开源模型
- [ ] 结构化原型：按 `Docs/prototype_fixture_design.md` 完成 V0.5 底板、传感器支架、TFT/按钮固定和线束整理，再开始正式 AI_SAMPLE 数据采集
